import React from "react";
import "./styles.scss";

const SiteHeader = (): JSX.Element => {
    return (
        <div id={"site-header"}>
            Site Header
        </div>
    );
};

export default SiteHeader;
