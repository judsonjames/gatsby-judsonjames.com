import React from "react";
import SiteLayout from "../../components/SiteLayout";

const Blog = (): JSX.Element => {
    return (
        <SiteLayout>
            <div>
                Blog Page
            </div>
        </SiteLayout>
    );
};

export default Blog;
