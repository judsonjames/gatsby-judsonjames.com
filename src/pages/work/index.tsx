import React from "react";
import SiteLayout from '../../components/SiteLayout';

const Work = (): JSX.Element => {
    return (
        <SiteLayout>
            <div>
                Work Page
            </div>
        </SiteLayout>
    );
};

export default Work;
